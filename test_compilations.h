#pragma once

#include <string>
#include <map>
#include <vector>
#include <math.h>
#include <algorithm>
#include <unordered_map>

namespace testexample {
	std::string testFunc();
	void quicksort(std::vector<int>& nums);
}
