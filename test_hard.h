#pragma once

#include <string>
#include <map>
#include <vector>
#include <algorithm>

namespace test_hard {
	bool test_10(std::string s, std::string p);
	std::vector<std::vector<int>> test_15(std::vector<int>& nums);
}
